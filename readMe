This repo contains the neural network based counterfactual regression implementation for Ad attribution. Simulated data has been used as the input to PrepareData.py which would be followed by Run.py.
